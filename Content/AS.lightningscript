// MIT Licensed (see LICENSE.md).

class AS : LightningComponent
{
  function Initialize(init : CogInitializer)
  {
    //Plasma.Connect(this.Space, Events.LogicUpdate, this.OnLogicUpdate);
  }

  function OnLogicUpdate(event : UpdateEvent)
  {
  }
}
